# Known Issues — بعد دفعة الواجهات

1. لا توجد `DATABASE_URL` في النسخة المرفقة؛ لذلك لم يتم تشغيل migrations أو
   اختبارات E2E الحقيقية.
2. شاشة العمليات وطلبات الإنتاج وإدارة عملاء البوابة أضيفت كملفات مستقلة، ولم
   تتم إضافة روابط لها إلى `public/index.html` في هذه الدفعة.
3. ما زال `production_orders` وملفاته القديمة موجودًا إلى أن يتم backup وdata
   audit وترحيل الصفوف المطلوبة.
4. لا يوجد بعد تقرير المرحلة 07 أو 08 أو 09، ولا يمكن اعتبار الإصدار جاهزًا
   للإنتاج قبل تنفيذها.
5. يظهر تحذير Vitest/Vite متعلق بتحميل ESM من config بصيغة CommonJS.