# تقرير إصلاح بوابة المبيعات الأونلاين

التاريخ: 2026-09-11

## نطاق التنفيذ

تم تنفيذ البرومبت الإصلاحي الأساسي الخاص بـ`portal` و`portal-orders` فقط.
تم استخدام تقرير الفحص العام كمرجع للسياق، ولم يتم تعديل قسم المبيعات أو
المدير التشغيلي.

## ما تم إصلاحه

### 1. منع تعارض مراجعة الإرسالية

- نقل قراءة الإرسالية وفحص المراجعة إلى نفس المعاملة (`transaction`).
- إضافة `SELECT ... FOR UPDATE` على أوامر الإنتاج المرتبطة بنفس `batchRef` في
  مساري التأكيد والرفض.
- الإبقاء على قيد قاعدة البيانات كشبكة أمان أخيرة.
- تحويل انتهاك القيد المحدد
  `portal_order_reviews_batch_ref_unique` إلى `409` برسالة واضحة، مع عدم
  ابتلاع أي `unique violation` أخرى.
- إضافة اختبار وحدة للتحقق من القيد، واختبار قبول يدوي يرسل طلبين متوازيين
  إلى السيرفر الحقيقي:

```bash
PORTAL_ORDERS_RACE_URL=https://your-domain.example \
PORTAL_ORDERS_RACE_TOKEN='employee-token' \
PORTAL_ORDERS_RACE_BATCH_REF='PORTAL-...' \
PORTAL_ORDERS_RACE_ACTION=confirm \
PORTAL_ORDERS_RACE_BODY='{"items":[{"workflowOrderId":1,"unitPrice":"10.00"}]}' \
node scripts/portal-orders-race-test.mjs
```

النتيجة المتوقعة: طلب واحد `200` والآخر `409` برسالة التعارض المحددة.

### 2. التعامل مع المنتجات المتوقفة

كان عمود `bom_recipes.is_active` موجودًا بالفعل في النسخة المرفقة عبر
`migrations/0040_bom_recipe_active.sql`، وكان موجودًا كذلك في schema وزر
التفعيل/الإيقاف في شاشة BOM؛ لذلك لم تتم إضافة migration مكررة أو تعديل
migration قديمة.

تم استكمال السلوك الناقص:

- `GET /portal/cart` يعيد `available: false` للمنتج المتوقف بدل إسقاطه من
  السلة.
- `GET /portal/wishlist` يعيد حالة الإتاحة للمنتج المتوقف.
- واجهة البوابة تعرض المنتج المتوقف بوضوح وتسمح بحذفه، لكنها تمنع تعديل كميته
  أو إرسال الطلب قبل إزالته.
- التحقق الخلفي الموجود في `POST /portal/cart` و`POST /portal/orders` بقي
  فعالًا، لذلك لا يمكن تجاوز الإيقاف باستدعاء API مباشر.
- تاريخ أوامر الإنتاج والفواتير لا يعتمد على حالة الكتالوج الحالية، وبالتالي
  لا يتأثر المنتج القديم المتوقف.

### 3. اتساق الصلاحيات

- تمت مراجعة الصفحات الست المحددة في البرومبت.
- لم يوجد تعارض فعلي بين HTML أو nav أو `PERMISSIONS` في النسخة المرفقة.
- أضيفت مصفوفة كاملة في `PORTAL-ROLES-CONSISTENCY-MATRIX.md`.
- أضيف `scripts/check-role-consistency.mjs`، ويعمل عبر:

```bash
npm run check:roles
```

يفشل السكربت برمز خروج `1` عند اختلاف أدوار HTML أو nav عن مصفوفة الباك إند.

## التحقق المنفذ

- `npm ci` — نجح بعد مزامنة metadata الاختيارية في `package-lock.json`.
- `npm run build` — نجح.
- `npm test` — نجح: **23 ملف اختبار، 137 اختبارًا ناجحًا، 3 اختبارات متخطاة**.
- `npm run check:roles` — نجح بدون تعارض.
- اختبار السباق الحقيقي على قاعدة بيانات/سيرفر إنتاج لم يتم تشغيله من داخل
  بيئة التسليم لعدم توفر رابط السيرفر وتوكن موظف و`batchRef` حي؛ تم تسليم
  سكربت التشغيل المباشر له دون تضمين أي بيانات اعتماد.

## إدارة حسابات العملاء

بناءً على طلب إضافة الحالتين، تمت إضافة:

- إيقاف/تفعيل حساب العميل من شاشة إدارة عملاء البوابة.
- إلغاء كل جلسات العميل فور إيقافه ومنع تسجيل الدخول للحساب المتوقف.
- حذف نهائي مشروط للحسابات المتوقفة فقط.
- الحذف النهائي يُرفض إذا كان للعميل طلب إنتاج أو فاتورة، مع توجيه الموظف
  لاستخدام الإيقاف للحفاظ على السجل التاريخي.
- تسجيل الإيقاف والتفعيل والحذف في سجل التدقيق.
- migration جديدة `0043_portal_customer_status.sql` لإضافة `is_active`.